export enum CellScale {
    none = 1,
    fit,
    fill,
    showAll,
    envelop,
    custom,
}

export enum CellAlign {
    none = 1,
    center,
    centerTop,
    centerBottom,
    leftCenter,
    leftTop,
    leftBottom,
    rightCenter,
    rightTop,
    rightBottom,
}
